import styled from 'styled-components';

export const List = styled.ul`
  // *
`;

export const ErrorText = styled.p`
  color: ${p => p.theme.colors.secondBtnColor};
  font-size: ${p => p.theme.fontSizes.s}px;
  font-weight: bold;
`;
